/** Board dimensions, kept dependency-free so both `main.ts` and the scenes can
 * import them without creating a circular module dependency. */
export const BOARD_WIDTH = 1000;
export const BOARD_HEIGHT = 720;
